---
// Rota do lado governo. E o MESMO componente da rota legada: ele detecta o
// lado pela URL e filtra o que mostra (ARQUITETURA-DOIS-PUBLICOS.md).
import Cases from "../../../components/paginas/Cases.astro";

export function getStaticPaths() {
  return [{ params: { lang: undefined } }, { params: { lang: "en" } }, { params: { lang: "es" } }];
}
---
<Cases />

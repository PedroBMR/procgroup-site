---
// Wrapper fino: o conteudo mora em src/components/paginas/SolucaoCidades.astro,
// instanciado tambem pela rota do lado (governo/empresas) — fase 3.
import SolucaoCidades from "../../../components/paginas/SolucaoCidades.astro";

export function getStaticPaths() {
  return [{ params: { lang: undefined } }, { params: { lang: "en" } }, { params: { lang: "es" } }];
}
---
<SolucaoCidades />

---
// Wrapper fino: o conteudo mora em src/components/paginas/SolucaoIaIndustrial.astro,
// instanciado tambem pela rota do lado (governo/empresas) — fase 3.
import SolucaoIaIndustrial from "../../../components/paginas/SolucaoIaIndustrial.astro";

export function getStaticPaths() {
  return [{ params: { lang: undefined } }, { params: { lang: "en" } }, { params: { lang: "es" } }];
}
---
<SolucaoIaIndustrial />

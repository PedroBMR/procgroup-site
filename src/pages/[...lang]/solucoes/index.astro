---
// Wrapper fino: o conteudo desta pagina mora em
// src/components/paginas/SolucoesIndex.astro, para ser instanciado pelos dois lados do site
// (governo/empresas) na fase 3 — ver ARQUITETURA-DOIS-PUBLICOS.md.
import SolucoesIndex from "../../../components/paginas/SolucoesIndex.astro";

export function getStaticPaths() {
  return [{ params: { lang: undefined } }, { params: { lang: "en" } }, { params: { lang: "es" } }];
}
---
<SolucoesIndex />
